# Watchdog モジュール仕様書

## 概要

vivive agent の自動監視・制御を行う expect モジュール。複数セッションの並列実行を支援し、音声通知による状態把握を実現する。

## モジュール構成

```
watchdog.exp
├── 状態検出モジュール
│   ├── 枠パターン検出
│   ├── 承認待機検出
│   └── 完了状態検出
├── 通知モジュール
│   ├── 音声通知
│   └── フラグファイル管理
├── 自動応答モジュール
│   ├── 承認自動応答
│   └── エラー時の待機
└── セッション管理モジュール
    ├── 初回起動処理
    └── 再アタッチ処理
```

## 状態定義

### 1. セッション状態

| 状態       | 説明                            | フラグファイル                        |
| ---------- | ------------------------------- | ------------------------------------- |
| `starting` | Claude Code 起動中              | なし                                  |
| `working`  | 作業中（出力が変化）            | なし                                  |
| `waiting`  | 入力待機中（30 秒以上変化なし） | `/tmp/claude_completed_$session_name` |
| `error`    | エラー検出（未知パターン）      | `/tmp/claude_error_$session_name`     |

### 2. 枠内状態パターン

| パターン             | 検出条件                        | アクション                |
| -------------------- | ------------------------------- | ------------------------- |
| `command_approval`   | "Do you want to proceed?"       | 自動 Enter 送信 + Ping 音 |
| `file_edit_approval` | "Do you want to make this edit" | 自動 Enter 送信 + Ping 音 |
| `bash_approval`      | "Bash command" など             | 自動 Enter 送信 + Ping 音 |
| `input_waiting`      | "? for shortcuts"               | 何もしない（正常状態）    |
| `unknown_pattern`    | 上記以外の枠パターン            | Basso 音 + 音声通知       |

## 通知仕様

### 1. 音声通知

| 通知タイプ | サウンド   | 音声メッセージ                                         | 発生条件              |
| ---------- | ---------- | ------------------------------------------------------ | --------------------- |
| 進行中     | Ping.aiff  | なし                                                   | 承認の自動応答時      |
| エラー     | Basso.aiff | "エージェント {番号} で未知の枠パターンを検出しました" | 未知パターン検出時    |
| 完了       | Glass.aiff | "エージェント {番号} が作業を完了しました"             | 30 秒間変化なし       |
| 再開       | Ping.aiff  | "エージェント {番号} が作業を再開しました"             | 待機 → 作業状態遷移時 |

### 2. フラグファイル

| ファイル                                                   | 目的             | 作成タイミング   | 削除タイミング |
| ---------------------------------------------------------- | ---------------- | ---------------- | -------------- |
| `/tmp/claude_completed_$session_name`                      | 完了状態の永続化 | 30 秒間変化なし  | 出力変化検出時 |
| `/tmp/claude_error_$session_name`                          | エラー状態の記録 | 未知パターン検出 | 手動削除       |
| `/tmp/claude_unknown_pattern_$session_name_$timestamp.log` | エラー詳細ログ   | 未知パターン検出 | 手動削除       |

## 動作モード

### 1. start モード（初回起動）

```
1. tmux セッション内で Claude Code を起動
2. 初期承認（フォルダ信頼など）を自動応答
3. Welcome メッセージ検出後、プロンプトを送信
4. 監視ループ開始
```

### 2. attach モード（再アタッチ）

```
1. 既存の tmux セッションに接続
2. 現在の状態を判定
3. 監視ループ開始（プロンプト送信はスキップ）
```

## 監視ループ仕様

```
while (true) {
    1. tmux ペインの内容をキャプチャ
    2. 出力変化の検出
    3. 状態遷移の判定
    4. 必要に応じて：
       - 自動応答の実行
       - 音声通知の発行
       - フラグファイルの更新
    5. 待機（1秒）
}
```

## エラーハンドリング

### 1. 未知パターンの検出

- 同一パターンは初回のみ通知（重複抑制）
- 詳細ログをファイルに保存
- ユーザー介入を待機

### 2. tmux セッション消失

- 正常終了として扱う
- ログファイルをクローズ

### 3. ユーザーアタッチ検出

- 自動応答を一時停止
- ユーザー操作を優先
- アタッチ/デタッチ状態の変化をログに記録
- デタッチ後は自動的に自動応答を再開

## ユーザーアタッチ時の動作

### 検出方法

```tcl
# tmux list-clients コマンドでアタッチ中のクライアント数を確認
proc is_user_attached {session_name} {
    catch {exec tmux list-clients -t $session_name 2>/dev/null | wc -l} count
    return [expr {$count > 0}]
}
```

### 状態遷移

```
ユーザーアタッチなし → 自動応答有効
    ↓ (ユーザーがアタッチ)
ユーザーアタッチ中 → 自動応答無効（ログに記録のみ）
    ↓ (ユーザーがデタッチ)
ユーザーアタッチなし → 自動応答有効（自動再開）
```

### ログ出力例

```
=== User attached to session - auto-approval DISABLED ===
=== Approval required (command_approval) - skipped due to user attachment ===
=== User detached from session - auto-approval ENABLED ===
```

## 使用例

### 初回起動

```bash
expect watchdog.exp start \
    /tmp/prompt.txt \
    /path/to/mcp.json \
    issue-123 \
    /tmp/log_123.log
```

### 再アタッチ

```bash
expect watchdog.exp attach \
    issue-123 \
    /tmp/log_reattach_123.log
```

## 設定可能パラメータ

| パラメータ   | デフォルト値                | 説明                   |
| ------------ | --------------------------- | ---------------------- |
| 監視間隔     | 1 秒                        | ループの待機時間       |
| 完了判定時間 | 30 秒                       | 変化なしと判定する時間 |
| タイムアウト | start: 15 分, attach: 30 分 | 監視の最大継続時間     |

## 依存関係

- tmux: セッション管理
- expect: 自動化スクリプト実行
- afplay: macOS 音声再生
- say: macOS 音声合成
